# Alembic configuration. The database URL is injected from app settings and
# logging is configured in alembic/env.py, so both are omitted here.
[alembic]
script_location = alembic
prepend_sys_path = .
path_separator = os
sqlalchemy.url =
