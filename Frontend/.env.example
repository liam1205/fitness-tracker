# Copy to .env (or .env.local) and adjust.
# Base URL of the backend API consumed by the generated client. Leave empty in
# dev so requests go through the Vite proxy (see vite.config.ts) instead of
# hitting the backend cross-origin, which keeps the session cookie same-site.
VITE_API_URL=
